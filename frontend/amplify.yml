version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step needed for static files"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
backend:
  phases:
    build:
      commands:
        - echo "Backend build"
  artifacts:
    baseDirectory: backend
    files:
      - '**/*'
