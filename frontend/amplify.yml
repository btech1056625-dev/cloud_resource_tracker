version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pre-build step"
    build:
      commands:
        - echo "Frontend static files build"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
